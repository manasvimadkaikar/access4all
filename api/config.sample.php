<?php
// api/config.sample.php
//
// SETUP:
// 1. Copy this file and rename the copy to "config.php" (same folder).
// 2. Get an API key from https://console.anthropic.com/ (Settings -> API Keys).
// 3. Paste it below, inside the quotes.
//
// config.php is already excluded by .gitignore, so your real key will
// never be committed to GitHub. Never share your API key or commit it
// to a public repository.

define('ANTHROPIC_API_KEY', 'your-api-key-here');
